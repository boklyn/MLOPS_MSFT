# This code is autogenerated.
# Code is generated by running custom script: python3 readme.py
# Any manual changes to this file may cause incorrect behavior.
# Any manual changes will be overwritten if the code is regenerated.

name: pipelines-jobs-mslearn-mlops1
on:
  push:
    branches: [ Data_Main ]
  workflow_dispatch:
  #schedule:
  #  - cron: "4 6/12 * * *"
  #pull_request:
  #  branches:
  #    - main
  #  paths:
  #    - cli/jobs/pipelines/nyc-taxi/**
  #    - infra/bootstrapping/**
  #    - .github/workflows/mslearn-mlops1.yml
  #    - cli/run-pipeline-jobs.sh
  #    - cli/setup.sh
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  buildProd:
    runs-on: ubuntu-latest
    environment:
        name: Prod 
    steps:
    - name: check out repo
      uses: actions/checkout@v4
    - name: azure login
      uses: azure/login@v2
      with:
        creds: ${{secrets.AZURE_CREDENTIALS}}
    - name: load datasets
      run: |
          az ml data create --version 2 --name diabetes-prod-folder --path data_prod --description "hello world"
      working-directory: cli/jobs/pipelines/mslearn-mlops
