# This code is autogenerated.
# Code is generated by running custom script: python3 readme.py
# Any manual changes to this file may cause incorrect behavior.
# Any manual changes will be overwritten if the code is regenerated.

name: pipelines-jobs-mslearn-mlops1
on:
  push:
    branches: [ main ]
  workflow_dispatch:
  #schedule:
  #  - cron: "4 6/12 * * *"
  #pull_request:
  #  branches:
  #    - main
  #  paths:
  #    - cli/jobs/pipelines/nyc-taxi/**
  #    - infra/bootstrapping/**
  #    - .github/workflows/mslearn-mlops1.yml
  #    - cli/run-pipeline-jobs.sh
  #    - cli/setup.sh
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  buildDev:
    runs-on: ubuntu-latest
    environment:
        name: Dev 
    steps:
    - name: check out repo
      uses: actions/checkout@v4
    - name: azure login
      uses: azure/login@v2
      with:
        creds: ${{secrets.AZURE_CREDENTIALS}}
    - name: bootstrap resources
      run: |
          echo '${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}';
          bash bootstrap.sh
      working-directory: infra/bootstrapping
      continue-on-error: false
    - name: setup-cli
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          bash setup.sh
      working-directory: cli
      continue-on-error: true
#    - name: load datasets
#      run: |
#          az ml data create --file mlops_dataset.yml
#      working-directory: cli/jobs/pipelines/mslearn-mlops
    - name: run job
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          bash -x ../../../run-job.sh job.yml
      working-directory: cli/jobs/pipelines/mslearn-mlops
    - name: validate readme
      run: |
          python check-readme.py "${{ github.workspace }}/cli/jobs/pipelines/mslearn-mlops"
      working-directory: infra/bootstrapping
      continue-on-error: false
  buildProd:
    runs-on: ubuntu-latest
    environment:
        name: Prod 
    needs: [ buildDev ]
    steps:
    - name: check out repo
      uses: actions/checkout@v4
    - name: azure login
      uses: azure/login@v2
      with:
        creds: ${{secrets.AZURE_CREDENTIALS}}
    - name: bootstrap resources
      run: |
          echo '${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}';
          bash bootstrap.sh
      working-directory: infra/bootstrapping
      continue-on-error: false
    - name: setup-cli
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          bash setup.sh
      working-directory: cli
      continue-on-error: true
#    - name: load datasets
#      run: |
#          az ml data create --file mlops_dataset.yml
#      working-directory: cli/jobs/pipelines/mslearn-mlops
    - name: run job
      run: |
          source "${{ github.workspace }}/infra/bootstrapping/sdk_helpers.sh";
          source "${{ github.workspace }}/infra/bootstrapping/init_environment.sh";
          bash -x ../../../run-job.sh job.yml
      working-directory: cli/jobs/pipelines/mslearn-mlops
    - name: validate readme
      run: |
          python check-readme.py "${{ github.workspace }}/cli/jobs/pipelines/mslearn-mlops"
      working-directory: infra/bootstrapping
      continue-on-error: false
